# Release lineage

## Internal build numbering

The ImageSaver family uses a shared internal build lineage.

- Next family build starts at `v2a`.
- Consecutive build numbers indicate sibling implementations based on the same shared specification.
- Public release versions are managed independently per repository.

## Current package

Project: `ComfyUI-PillowImageSaver`

Public version: `0.1.0`

Internal build: `v2a`

Backend: `pillow`
